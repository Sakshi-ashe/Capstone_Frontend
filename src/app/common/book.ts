

export class Book {

    // below properties should match with
    // JSON data returned from REST API
    book_id: string;
    title: string;
    description: string;
    contributor: number;
    imageUrl : string;
    author: boolean;
}
